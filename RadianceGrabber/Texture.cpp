#include "Texture.h"

namespace RadGrabber
{
	Texture::Texture()
	{
	}

	Texture::Texture(void * p, int pixelSize, int pixelCount)
	{
	}

	Texture::Texture(const Texture & t)
	{
	}

	Texture::Texture(const Texture && t)
	{
	}

	Texture::Texture(const TextureChunk * c)
	{
	}


	Texture::~Texture()
	{
	}
}